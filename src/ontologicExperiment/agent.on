communication:
    sensor("answerSensor", "ontologicExperiment.sensors.AnswerSensor").
	actuator("buildQuestion", "ontologicExperiment.actuators.BuildQuestion").

beliefs:
    
desires:

intentions:
	questionAbout(malta,rigth).
	
_ontologic: 

! desires questionAbout(X,rigth) :- _ontologic newResource(X).

! _ontologic question(X) :- intentions questionAbout(X,rigth).

! communication buildQuestion(X,Y) :- _ontologic officialLanguage(malta,X) & _ontologic officialLanguage(malta,Y).